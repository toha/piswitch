#include "txhelper.h"
#define TX_PIN 8;
/*
void tx(int pulse_length, int num_high_pulses, int num_low_pulses) {
	if (num_high_pulses > 0) {
		digitalWrite(8, HIGH);
		delayMicroseconds( pulse_length * num_high_pulses);
	}
	if (num_low_pulses > 0) {
		digitalWrite(8, LOW);
		delayMicroseconds( pulse_length * num_low_pulses);
	}
    
}*/

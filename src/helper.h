#ifndef _HELPER_H_
#define _HELPER_H_

#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <sys/time.h>

void printBits(size_t const size, void const * const ptr);
unsigned long getMicros();


#endif
